import AppShell from "./components/layout/AppShell";

// The five-zone shell (DESIGN.md §8). Zones are wired to stubbed IPC; bodies fill in build-order.
export default function App() {
  return <AppShell />;
}
